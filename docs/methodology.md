# Methodology

The MVP uses RDKit descriptors, molecular fingerprints, transparent similarity metrics, and heuristic scoring. These are research prioritization tools, not experimentally validated predictions.
