# Explainability

AZAI emphasizes transparent outputs. In v0.4.0, explainability includes:

- descriptor interpretation tables;
- baseline model feature importance where supported;
- explicit warnings when models are exploratory rather than validated.

Descriptor explanations are heuristic scientific notes, not causal model attributions. SHAP and atom-level attribution are planned for later versions once realistic experimental datasets are available.
